class Agendamento {
    constructor() {
        this._agendamento = [];
    }
    adiciona(agendamento) {
        this._agendamento.push(agenda);
    }
    paraArray() {
        return [].concat(this._agendamento);
    }
}
